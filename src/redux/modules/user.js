const initialState = {
  orders: {
    isFetching: false,
    ids: [], // 已完成订单
    toPayIds: [], // 待付款
    reFundIds: [], // 退款订单
    availableIds: [] // 可使用的订单
  },
  currentType: 0
};

export const types

// 外部访问的actions
export const actions = {

}